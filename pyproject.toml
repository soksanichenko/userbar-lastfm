[project]
name = "lastfm-userbar"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.1",
    "Pillow>=12.0",
    "requests>=2.34",
    "gunicorn>=26.0",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
